/** Decorative SVGs in /public/graphics - recolor via #A10000 / #B10000 / #C10000 (a/b/c). */

export const GRAPHICS = [
  {
    id: 'spark-mini',
    src: 'spark-mini.svg',
    label: 'Sparks',
    defaultPlacement: { left: 48, top: 120, width: 200 },
  },
  {
    id: 'sparkles',
    src: 'sparkles.svg',
    label: 'Sparkles',
    defaultPlacement: { left: 0, top: 0, width: 1080 },
  },
];

/** Functional icons in /public/graphics/icons/ - same recolor system. */
export const ICONS = [
  {
    id: 'icon-check',
    src: 'icons/check-circle.svg',
    label: 'Check',
    use: 'Feature checkmark, validation',
    defaultPlacement: { left: 80, top: 400, width: 120 },
  },
  {
    id: 'icon-star',
    src: 'icons/star.svg',
    label: 'Star',
    use: 'Rating, featured, premium',
    defaultPlacement: { left: 80, top: 400, width: 120 },
  },
  {
    id: 'icon-heart',
    src: 'icons/heart.svg',
    label: 'Heart',
    use: 'Favorites, likes, social',
    defaultPlacement: { left: 80, top: 400, width: 120 },
  },
  {
    id: 'icon-rocket',
    src: 'icons/rocket.svg',
    label: 'Rocket',
    use: 'Launch, speed, new',
    defaultPlacement: { left: 80, top: 400, width: 120 },
  },
  {
    id: 'icon-bolt',
    src: 'icons/bolt.svg',
    label: 'Bolt',
    use: 'Fast, instant, power',
    defaultPlacement: { left: 80, top: 400, width: 120 },
  },
  {
    id: 'icon-shield',
    src: 'icons/shield.svg',
    label: 'Shield',
    use: 'Security, privacy, safe',
    defaultPlacement: { left: 80, top: 400, width: 120 },
  },
  {
    id: 'icon-search',
    src: 'icons/search.svg',
    label: 'Search',
    use: 'Discover, find, explore',
    defaultPlacement: { left: 80, top: 400, width: 120 },
  },
  {
    id: 'icon-camera',
    src: 'icons/camera.svg',
    label: 'Camera',
    use: 'Photo, scan, capture',
    defaultPlacement: { left: 80, top: 400, width: 120 },
  },
  {
    id: 'icon-chat',
    src: 'icons/chat.svg',
    label: 'Chat',
    use: 'Message, talk, support',
    defaultPlacement: { left: 80, top: 400, width: 120 },
  },
  {
    id: 'icon-settings',
    src: 'icons/settings.svg',
    label: 'Settings',
    use: 'Preferences, configure',
    defaultPlacement: { left: 80, top: 400, width: 120 },
  },
  {
    id: 'icon-calendar',
    src: 'icons/calendar.svg',
    label: 'Calendar',
    use: 'Schedule, time, events',
    defaultPlacement: { left: 80, top: 400, width: 120 },
  },
  {
    id: 'icon-map-pin',
    src: 'icons/map-pin.svg',
    label: 'Map pin',
    use: 'Location, navigation, places',
    defaultPlacement: { left: 80, top: 400, width: 120 },
  },
  {
    id: 'icon-wallet',
    src: 'icons/wallet.svg',
    label: 'Wallet',
    use: 'Payment, finance, money',
    defaultPlacement: { left: 80, top: 400, width: 120 },
  },
  {
    id: 'icon-crown',
    src: 'icons/crown.svg',
    label: 'Crown',
    use: 'VIP, premium, exclusive',
    defaultPlacement: { left: 80, top: 400, width: 120 },
  },
  {
    id: 'icon-target',
    src: 'icons/target.svg',
    label: 'Target',
    use: 'Goals, focus, objectives',
    defaultPlacement: { left: 80, top: 400, width: 120 },
  },
  {
    id: 'icon-trophy',
    src: 'icons/trophy.svg',
    label: 'Trophy',
    use: 'Achievement, reward, success',
    defaultPlacement: { left: 80, top: 400, width: 120 },
  },
  {
    id: 'icon-clock',
    src: 'icons/clock.svg',
    label: 'Clock',
    use: 'History, recent, timer',
    defaultPlacement: { left: 80, top: 400, width: 120 },
  },
  {
    id: 'icon-bookmark',
    src: 'icons/bookmark.svg',
    label: 'Bookmark',
    use: 'Save, remember, collection',
    defaultPlacement: { left: 80, top: 400, width: 120 },
  },
  {
    id: 'icon-download',
    src: 'icons/download.svg',
    label: 'Download',
    use: 'Download CTA, save',
    defaultPlacement: { left: 80, top: 400, width: 120 },
  },
  {
    id: 'icon-play',
    src: 'icons/play.svg',
    label: 'Play',
    use: 'Video, demo, media',
    defaultPlacement: { left: 440, top: 800, width: 200 },
  },
  {
    id: 'icon-arrow-right',
    src: 'icons/arrow-right.svg',
    label: 'Arrow',
    use: 'Next step, more, CTA',
    defaultPlacement: { left: 80, top: 900, width: 140 },
  },
  {
    id: 'icon-arrow-up',
    src: 'icons/arrow-up.svg',
    label: 'Up arrow',
    use: 'Growth, upload, increase',
    defaultPlacement: { left: 80, top: 400, width: 120 },
  },
  {
    id: 'icon-gift',
    src: 'icons/gift.svg',
    label: 'Gift',
    use: 'Free, bonus, promotion',
    defaultPlacement: { left: 80, top: 400, width: 120 },
  },
  {
    id: 'icon-bell',
    src: 'icons/bell.svg',
    label: 'Bell',
    use: 'Notifications, alerts',
    defaultPlacement: { left: 80, top: 400, width: 120 },
  },
  {
    id: 'icon-share',
    src: 'icons/share.svg',
    label: 'Share',
    use: 'Sharing, social, viral',
    defaultPlacement: { left: 80, top: 400, width: 120 },
  },
  {
    id: 'icon-lock',
    src: 'icons/lock.svg',
    label: 'Lock',
    use: 'Privacy, secure, premium',
    defaultPlacement: { left: 80, top: 400, width: 120 },
  },
  {
    id: 'icon-globe',
    src: 'icons/globe.svg',
    label: 'Globe',
    use: 'Worldwide, languages, web',
    defaultPlacement: { left: 80, top: 400, width: 120 },
  },
];

export const SLOT_COLORS = {
  a: '#00A',
  b: '#00B',
  c: '#00C',
};

export function getGraphicById(id) {
  return GRAPHICS.find((g) => g.id === id) || ICONS.find((g) => g.id === id) || null;
}

export function getGraphicBySrc(src) {
  const file = (src || '').split('/').pop();
  return GRAPHICS.find((g) => g.src === file) || ICONS.find((g) => g.src.endsWith(file)) || null;
}

// ─── Color contrast helpers ─────────────────────────────────────────────────

/** Parse hex color to [r, g, b] (0-255). */
function hexToRgb(hex) {
  const h = hex.replace('#', '');
  const n = parseInt(h, 16);
  return [(n >> 16) & 255, (n >> 8) & 255, n & 255];
}

/** Relative luminance per WCAG 2.0. 0 = dark, 1 = light. */
export function luminance(hex) {
  const [r, g, b] = hexToRgb(hex).map((c) => {
    const s = c / 255;
    return s <= 0.03928 ? s / 12.92 : ((s + 0.055) / 1.055) ** 2.4;
  });
  return 0.2126 * r + 0.7152 * g + 0.0722 * b;
}

/** Is this color dark? (luminance < 0.4) */
export function isDark(hex) {
  return luminance(hex) < 0.4;
}

/**
 * Pick a visible icon color against a background.
 * If the theme color contrasts with bg, use it.
 * Otherwise fall back to white (dark bg) or dark (light bg).
 */
export function contrastingIconColor(themeColor, bgColor) {
  if (!bgColor) return themeColor;
  const themeDark = isDark(themeColor);
  const bgDark = isDark(bgColor);
  // If they're on opposite sides of dark/light, theme color is visible
  if (themeDark !== bgDark) return themeColor;
  // Same side — pick the opposite
  return bgDark ? '#FFFFFF' : '#1A1A2E';
}
